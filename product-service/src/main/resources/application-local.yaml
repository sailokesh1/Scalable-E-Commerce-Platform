spring:
  data:
    mongodb:
      username: ${MONGO_USER:user}
      password: ${MONGO_PASSWORD:password}
      database: ${MONGO_DB:Products}
      port: ${MONGO_PORT:27017}
      host: ${MONGO_HOST:localhost}
      authentication-database: ${AUTH_SOURCE:admin}


